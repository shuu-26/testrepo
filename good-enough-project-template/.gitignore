# exclude compiled binaries and external models
/bin

# exclude data from source control by default
/data/temp
/data/processed
/data/raw

# exclude .Rhistory files from source control by default
/src/.Rhistory
